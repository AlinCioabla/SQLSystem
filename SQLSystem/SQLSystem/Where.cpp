#include "stdafx.h"
#include "Where.h"

Where::Where()
{
}

IState * Where::HandleToken(TokenPtr &   aCurrentToken,
                            TokenPtr &   prevToken,
                            AstNodePtr & aCurrentInstructionNode,
                            Ast &        aAst)
{
  return nullptr;
}

Where::~Where()
{
}
