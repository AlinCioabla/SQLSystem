#include "stdafx.h"
#include "Valid.h"

Valid::Valid()
{
}

IState * Valid::HandleToken(TokenPtr &   aCurrentToken,
                            TokenPtr &   prevToken,
                            AstNodePtr & aCurrentInstructionNode,
                            Ast &        aAst)
{
  return nullptr;
}

Valid::~Valid()
{
}
