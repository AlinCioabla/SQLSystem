#include "stdafx.h"
#include "Distinct.h"

Distinct::Distinct()
{
}

IState * Distinct::HandleToken(TokenPtr &   aCurrentToken,
                               TokenPtr &   prevToken,
                               AstNodePtr & aCurrentInstructionNode,
                               Ast &        aAst)
{
  return nullptr;
}

Distinct::~Distinct()
{
}
